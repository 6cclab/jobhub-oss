# JobHub — Claude Code

Read [`AGENTS.md`](../AGENTS.md) at the repo root. It holds the project instructions.

- Edit commands and rules in `prompts/`, never through the `.claude/commands` or `.claude/rules`
  symlinks.
- Put machine-local settings in `.claude/settings.local.json`. Keep personal hosts and tokens out
  of `.claude/settings.json`.
