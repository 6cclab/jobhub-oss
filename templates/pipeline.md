# Pipeline

Cross-application strategy: which roles you'd take over which, what each channel has actually
produced, and any timing collisions between loops. Hand-written — nothing generates this.

Per-application detail belongs in `user/applications/{company}-{role}.md`, not here.
